{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "cppvsdbg",
            "request": "launch",
            "name": "ams2_championship_server (serve on :8080)",
            "program": "${workspaceFolder}/target/debug/ams2_championship_server.exe",
            "args": ["8080"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "preLaunchTask": "cargo build"
        }
    ]
}
